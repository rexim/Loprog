language(scala).
language(haskell).
language(ocaml).
language(c_plus_plus).
language(java).
language(pascal).

functional(scala).
functional(haskell).
functional(ocaml).

oop(scala).
oop(ocaml).
oop(c_plus_plus).
oop(java).

equal(X, X).

last(cons(Last, nil), Last).

last(cons(X, Xs), Last) :-
    last(Xs, Last).
